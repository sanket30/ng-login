(function (angular) {
    'use strict';

    angular
        .module('app.benefitsPackage')
        .controller('BenefitsPackageController', BenefitsPackageController);

    // @ngInject
    function BenefitsPackageController() {
        var vm = this;

    }
}(angular));